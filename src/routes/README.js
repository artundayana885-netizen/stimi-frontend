/**
 * routes/
 *
 * Carpeta reservada para la configuración de React Router DOM cuando se
 * integre navegación por URL (por ejemplo /login, /coordinador, /instructor,
 * /informes/:id). Hoy la navegación entre vistas se maneja con estado en
 * App.jsx y en cada dashboard (CoordinatorDashboard / InstructorDashboard).
 */
export {};
