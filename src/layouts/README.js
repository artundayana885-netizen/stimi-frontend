/**
 * layouts/
 *
 * Carpeta reservada para layouts compartidos (por ejemplo, un
 * `DashboardLayout` que agrupe el sidebar + el contenedor principal que hoy
 * están repetidos dentro de CoordinatorDashboard e InstructorDashboard).
 *
 * Por ahora cada dashboard mantiene su propio layout interno para no romper
 * nada; cuando se quiera extraer un layout común, este es el lugar.
 */
export {};
